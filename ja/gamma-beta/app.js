'use strict';
const $=id=>document.getElementById(id);
const fmt=n=>Number.isFinite(n)?(Math.abs(n)>=1e5||Math.abs(n)<1e-4?n.toExponential(6):Number(n.toPrecision(7)).toString()):"数値範囲外";
function lgamma(z){const c=[676.5203681218851,-1259.1392167224028,771.3234287776531,-176.61502916214059,12.507343278686905,-.13857109526572012,9.984369578019572e-6,1.5056327351493116e-7];if(z<.5)return Math.log(Math.PI)-Math.log(Math.sin(Math.PI*z))-lgamma(1-z);z-=1;let x=.9999999999998099;c.forEach((v,i)=>x+=v/(z+i+1));let t=z+7.5;return .5*Math.log(2*Math.PI)+(z+.5)*Math.log(t)-t+Math.log(x)}
const gamma=s=>Math.exp(lgamma(s));const beta=(a,b)=>Math.exp(lgamma(a)+lgamma(b)-lgamma(a+b));
function tex(el,value,display=true){katex.render(value,typeof el==='string'?$(el):el,{displayMode:display,throwOnError:true})}
function typeset(el=document.body){renderMathInElement(el,{delimiters:[{left:'$$',right:'$$',display:true},{left:'\\(',right:'\\)',display:false}],throwOnError:true})}
let family='gamma', s=2, a=2, b=3;
function controls(){const params=family==='gamma'?[['s',"形状 s",s]]:[['a',"左パラメータ a",a],['b',"右パラメータ b",b]];$('parameter-controls').innerHTML=params.map(([id,label,v])=>`<label for="param-${id}"><span class="control-head"><span>${label}</span><output id="value-${id}">${v.toFixed(2)}</output></span><input id="param-${id}" type="range" min="0.25" max="6" step="0.05" value="${v}"></label>`).join('');params.forEach(([id])=>$('param-'+id).addEventListener('input',e=>{const v=+e.target.value;if(id==='s')s=v;if(id==='a')a=v;if(id==='b')b=v;$('value-'+id).value=v.toFixed(2);draw()}));const presets=family==='gamma'?[["半分",.5],["指数関数",1],["階乗",4]]:[["一様",1,1],["対称",3,3],["端点でのピーク",.5,.5]];$('presets').innerHTML=presets.map(([label],i)=>`<button type="button" data-preset="${i}">${label}</button>`).join('');$('presets').querySelectorAll('button').forEach((btn,i)=>btn.onclick=()=>{if(family==='gamma')s=presets[i][1];else[a,b]=presets[i].slice(1);controls();draw()})}
function draw(){const g=family==='gamma',value=g?gamma(s):beta(a,b);tex('kernel',g?String.raw`\Gamma(s)=\int_0^\infty t^{s-1}e^{-t}\,dt`:String.raw`B(a,b)=\int_0^1 t^{a-1}(1-t)^{b-1}\,dt`);$('area-value').textContent=fmt(value);tex('area-identity',g?`\\Gamma(${s.toFixed(2)})`:`B(${a.toFixed(2)},${b.toFixed(2)})`,false);$('shape-note').textContent=g?(s<1?"ゼロ付近で曲線は無限に上昇しますが、s > 0 のため面積は有限になります。":s===1?"s = 1 では被積分関数は e⁻ᵗ になります。その総面積は 1 です。":`t = s − 1 = ${fmt(s-1)} で被積分関数がピークをとります。正の整数では Γ(s) = (s − 1)! です。`):'Parameter a controls the behavior near 0; b controls it near 1. Swapping a and b reflects the curve and preserves its area.';
const svg=$('area-plot'),W=svg.clientWidth,H=svg.clientHeight,m={l:58,r:16,t:24,b:44},xmax=g?Math.max(8,s+5*Math.sqrt(s)):1,fn=t=>g?Math.exp((s-1)*Math.log(t)-t):Math.exp((a-1)*Math.log(t)+(b-1)*Math.log1p(-t));let data=Array.from({length:701},(_,i)=>{let t=xmax*(.001+.998*i/700);return[t,fn(t)]});let ymax=Math.max(...data.map(d=>d[1]))*1.12;const X=x=>m.l+x/xmax*(W-m.l-m.r),Y=y=>H-m.b-y/ymax*(H-m.t-m.b),base=H-m.b;let line=data.map(([x,y],i)=>`${i?'L':'M'}${X(x).toFixed(2)},${Y(y).toFixed(2)}`).join(' '),area=`${line} L${X(data.at(-1)[0])},${base} L${X(data[0][0])},${base} Z`;let ticks='';for(let i=0;i<=4;i++){let yy=ymax*i/4;ticks+=`<line x1="${m.l}" x2="${W-m.r}" y1="${Y(yy)}" y2="${Y(yy)}" stroke="var(--line)"/><text x="${m.l-9}" y="${Y(yy)+4}" text-anchor="end">${Number(yy.toPrecision(2))}</text>`}for(let i=0;i<=4;i++){let xx=xmax*i/4;ticks+=`<text x="${X(xx)}" y="${base+22}" text-anchor="middle">${Number(xx.toPrecision(2))}</text>`}svg.setAttribute('viewBox',`0 0 ${W} ${H}`);svg.setAttribute('aria-label',`${g?"ガンマ関数":"ベータ関数"} の被積分関数で全面積 ${fmt(value)}。プロットでは端点を除外しています${g?" かつ無限な尾を切り捨てます":''}。`);svg.innerHTML=`<title>${g?"ガンマ関数":"ベータ関数"} の被積分関数</title><desc> シャドーされた有限な範囲が積分を示しています。全値は独立に計算され、除外された端点および任意の尾を含みます。</desc>${ticks}<path d="${area}" fill="${g?'var(--blue)':'var(--purple)'}" opacity="0.13"/><path d="${line}" stroke="${g?'var(--blue)':'var(--purple)'}" fill="none" stroke-width="2.5"/><text x="${m.l}" y="14">被積分関数</text><text x="${W-m.r}" y="${H-4}" text-anchor="end">t</text>`;$('plot-note').textContent=g?`プロット範囲: 0.001T ≤ t ≤ 0.999T, T = ${xmax.toFixed(2)}。表示されている全積分は、除外された端点および無限な尾を含みます。軸は再スケーリングされます。`:'Plot window: 0.001 ≤ t ≤ 0.999. Endpoint singularities are cropped; the displayed full integral includes them. Axes rescale.'}
['gamma','beta'].forEach(f=>$(f+'-tab').onclick=()=>{family=f;['gamma','beta'].forEach(t=>$(t+'-tab').setAttribute('aria-pressed',t===f));controls();draw()});controls();new ResizeObserver(draw).observe($('area-plot'));draw();

const specs={exp:[['p',4],['c',2],['r',1]],finite:[['a',1.5],['b',2.5],['L',2]],rational:[['p',3],['q',3],['c',1],['r',2]],trig:[['m',4],['n',2]]};
function setupTransform(){const kind=$('integral-family').value;$('integral-params').innerHTML=specs[kind].map(([k,v])=>`<label for="int-${k}">${k}<input type="number" step="0.25" id="int-${k}" value="${v}"></label>`).join('');$('integral-params').querySelectorAll('input').forEach(el=>el.oninput=transform);transform()}
function transform(){const kind=$('integral-family').value;let v={},missing=false;specs[kind].forEach(([k])=>{const el=$('int-'+k);v[k]=el.valueAsNumber;if(!Number.isFinite(v[k]))missing=true});let eq='',sub='',match='',reason='',value=NaN,valid=true;
if(missing){$('convergence').textContent="すべてのフィールドに有限な数を入力してください。";$('convergence').classList.add('invalid');['input-equation','substitution','matched-equation','calculated','transform-explanation'].forEach(id=>$(id).textContent='');return}
const {p,q,c,r,L,a,b,m,n}=v;
if(kind==='exp'){eq=String.raw`\int_0^\infty x^{${p-1}}e^{-${c}x^{${r}}}\,dx`;sub=String.raw`t=${c}x^{${r}},\qquad x^{p-1}dx=\frac{t^{p/r-1}}{r c^{p/r}}dt`;valid=p>0&&c>0&&r>0;reason=c<=0||r<=0?"このテンプレートの外では、c および r は正でなければなりません。":p<=0?"ゼロで発散: べき p − 1 は −1 を超えなければなりません。":"収束: p > 0, c > 0, r > 0。";match=String.raw`\frac{\Gamma(p/r)}{r c^{p/r}}=\frac{\Gamma(${fmt(p/r)})}{${r}\cdot ${c}^{${fmt(p/r)}}}`;if(valid)value=Math.exp(lgamma(p/r)-Math.log(r)-p/r*Math.log(c));$('transform-explanation').textContent='The new Gamma argument is p/r. The exponential takes care of infinity; the power at zero supplies the restriction.'}
if(kind==='finite'){eq=String.raw`\int_0^{${L}}x^{${a-1}}(${L}-x)^{${b-1}}\,dx`;sub=String.raw`x=${L}t,\qquad dx=${L}\,dt`;valid=a>0&&b>0&&L>0;reason=L<=0?"このテンプレートの外では L は正でなければなりません。":!valid?"端点で発散: a および b はどちらも正でなければなりません。":"収束: a > 0, b > 0, L > 0。";match=String.raw`L^{a+b-1}B(a,b)=${L}^{${fmt(a+b-1)}}B(${a},${b})`;if(valid)value=Math.exp((a+b-1)*Math.log(L)+lgamma(a)+lgamma(b)-lgamma(a+b));$('transform-explanation').textContent='The two endpoint factors contribute Lᵃ⁻¹ and Lᵇ⁻¹; dx contributes one more L.'}
if(kind==='rational'){eq=String.raw`\int_0^\infty\frac{x^{${p-1}}}{(1+${c}x^{${r}})^{${q}}}\,dx`;sub=String.raw`t=${c}x^{${r}}`;valid=c>0&&r>0&&p>0&&p<r*q;reason=c<=0||r<=0?"このテンプレートの外では、c および r は正でなければなりません。":p<=0?"ゼロで発散: p は正でなければなりません。":p>=r*q?"無限大で発散：p は r × q より小さい必要がある。":`収束：0 < p < rq (${fmt(r*q)})。`;match=String.raw`\frac{B(p/r,q-p/r)}{r c^{p/r}}=\frac{B(${fmt(p/r)},${fmt(q-p/r)})}{${r}\cdot${c}^{${fmt(p/r)}}}`;if(valid)value=Math.exp(lgamma(p/r)+lgamma(q-p/r)-lgamma(q)-Math.log(r)-p/r*Math.log(c));$('transform-explanation').textContent="ベータ関数の引数は分母のべき乗 q に合計される。無限大で元のべきは p − 1 − rq。"}
if(kind==='trig'){eq=String.raw`\int_0^{\pi/2}\sin^{${m}}\theta\cos^{${n}}\theta\,d\theta`;sub=String.raw`t=\sin^2\theta,\qquad d\theta=\frac{dt}{2\sqrt{t(1-t)}}`;valid=m>-1&&n>-1;reason=valid?"収束：m > −1 かつ n > −1。":"端点で発散：m および n は −1 を超える必要がある。";match=String.raw`\frac12 B\!\left(\frac{m+1}2,\frac{n+1}2\right)=\frac12 B(${fmt((m+1)/2)},${fmt((n+1)/2)})`;if(valid)value=.5*beta((m+1)/2,(n+1)/2);$('transform-explanation').textContent="微分は因子 1/2 を与え、変換されたべきにそれぞれ 1/2 を引く。"}
tex('input-equation',eq);tex('substitution',sub);$('convergence').textContent=reason;$('convergence').classList.toggle('invalid',!valid);$('match-title').textContent=valid?"一致":"まず領域を確認する";if(valid){tex('matched-equation',match);$('calculated').textContent=Number.isFinite(value)&&value>0?'≈ '+fmt(value):"数値範囲外";}else{$('matched-equation').textContent="ガンマ/ベータの評価はこれらのパラメータに対して有効ではない。";$('calculated').textContent=''}
}
$('integral-family').onchange=setupTransform;setupTransform();
let selectedExample=0,selectedStep=0;
$('example-list').innerHTML=EXAMPLES.map((e,i)=>`<button type="button" data-example="${i}" aria-pressed="${i===0}">${String(i+1).padStart(2,'0')} · ${e.title}</button>`).join('');
function showExample(){const e=EXAMPLES[selectedExample],step=e.steps[selectedStep];$('example-tag').textContent=e.tag;tex('example-integral',e.integral);$('example-condition').textContent=e.condition;$('step-title').textContent=step[0];tex('step-equation',step[1]);$('step-text').textContent=step[2];$('step-counter').textContent=`ステップ ${selectedStep+1} ของ ${e.steps.length}`;$('previous-step').disabled=selectedStep===0;$('next-step').disabled=selectedStep===e.steps.length-1;$('example-list').querySelectorAll('button').forEach((btn,i)=>btn.setAttribute('aria-pressed',i===selectedExample))}
$('example-list').querySelectorAll('button').forEach((btn,i)=>btn.onclick=()=>{selectedExample=i;selectedStep=0;showExample()});$('previous-step').onclick=()=>{if(selectedStep>0)selectedStep--;showExample()};$('next-step').onclick=()=>{if(selectedStep<EXAMPLES[selectedExample].steps.length-1)selectedStep++;showExample()};showExample();
$('practice-items').innerHTML=PRACTICE.map(([problem,hint,answer],i)=>`<div><h3>${String(i+1).padStart(2,'0')}</h3><div class="equation" data-practice-problem="${i}"></div><details><summary>Hint</summary><div class="equation" data-practice-hint="${i}"></div></details><details><summary>Solution</summary><div class="equation" data-practice-answer="${i}"></div></details></div>`).join('');PRACTICE.forEach(([p,h,a],i)=>{tex(document.querySelector(`[data-practice-problem="${i}"]`),p);tex(document.querySelector(`[data-practice-hint="${i}"]`),h,false);tex(document.querySelector(`[data-practice-answer="${i}"]`),a)});typeset();
